packages:
  - "apps/*"
  - "packages/*"

# Build scripts allowed to run on install (pnpm blocks the rest by default).
onlyBuiltDependencies:
  - "@biomejs/biome"
  - "@tailwindcss/oxide"
  - "sharp"
  - "unrs-resolver"
