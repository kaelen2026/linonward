# Copy to apps/worker/.env — that file is gitignored.

REDIS_URL=redis://localhost:6379
DATABASE_URL=postgres://linonward:linonward@localhost:5432/linonward

# Keep separate deployments on the same Redis instance isolated.
QUEUE_PREFIX=linonward

# Number of jobs this process may execute concurrently.
WORKER_CONCURRENCY=5
