<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext stays off everywhere except the addresses a development build
  reaches the local API on: the emulator's alias for the host loopback, and the
  device's own loopback for `adb reverse`. This is the
  Android half of `NSAllowsLocalNetworking` in `apps/ios` — scoped to loopback,
  so a release talking to a real host is still held to HTTPS. A blanket
  `cleartextTrafficPermitted="true"` would drop that everywhere and is the
  wrong fix for a connection failure.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
